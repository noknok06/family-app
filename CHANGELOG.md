# CHANGELOG

このプロジェクトの特筆すべき変更を記録する。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠。
変更を merge するたびに `Unreleased` へ 1 行追記し、節目で日付付きセクションへ切り出す（[CONTRIBUTING.md](./CONTRIBUTING.md)）。

## [Unreleased]

### Added
- 旅行の行程からお出かけリストの場所を Google マップで開けるように。行程の「場所」がお出かけリストの場所名と一致する場合、その行に「地図」ボタンを表示し、登録済みの座標（なければ住所・場所名）でマップを開く。行程フォームの場所欄ではお出かけリストの場所名を入力候補として選べ、一致しているかをその場で確認できる（一致判定は全角/半角・大小文字・空白の違いを無視）
- 旅行記録の日本地図を上下スワイプで開閉できるように（`travel/MapPanel`）。地図の下のハンドルを上下にスワイプすると指に追従して開閉し、地図の上を直接スワイプしても閉じられる（拡大中や地図がはみ出しているときは、地図のドラッグ移動 / スクロールを優先）。ハンドルのタップと従来の「地図を隠す」ボタンでも切り替えられる
- 旅行の宿泊先を Google マップから検索して設定できるように（マイグレーション 035）。候補を選ぶと施設名・住所・座標を保存し、概要から「地図で開く」で Google マップを開ける。API キー未設定時は通常のテキスト入力として動作する
- 旅行の費用に 1 人あたりの金額を追加。参加人数は同行者（選んだ家族メンバー + 家族以外の同行者を「、」区切りで数えた数）から自動で求め、予算と行程費用を割った額を概要に表示する
- 旅行の同行者を家族メンバーから選べるように（マイグレーション 034）。招待済みメンバーをチップで複数選択でき（予定表と同じメンバー色）、旅行の概要にも名前で表示される。家族以外の同行者は従来どおり自由入力欄に書ける
- 旅行記録の日本地図を拡大縮小できるように（`JapanMap`）。ピンチ / Ctrl+ホイール / ＋−ボタンで拡大し、拡大中はドラッグで移動、リセットで元に戻せる。`viewBox` を操作する方式のためページ全体の表示倍率には影響せず、等倍のときは地図の上でもページを縦スクロールできる
- 日本地図が画面の横幅を使うように。従来は幅 220px（480px 以上で 300px）に固定されていたため、スマホでは左右に大きな余白ができていた。表示枠を横幅いっぱい（最大 560px）に広げ、`viewBox` の縦横比を枠に合わせて追従させることで、拡大したときに枠の幅をすべて使えるようにした。あわせて地図データ上下の余白を除いて描画するようにし、iPhone 相当の幅で従来比 約 1.4 倍の大きさになった
- 旅行記録を「旅行計画・記録」に拡張し、準備から当日の実行までを 1 つの旅行にまとめられるように（マイグレーション 033）。旅行に同行者・交通手段・宿泊先・予算を持たせ、詳細を「概要 / 準備 / 行程」の 3 タブに再構成。準備は持ち物・やることのチェックリスト（担当者・進捗メーター、`travel_prep_items`）、行程は日ごとの時刻・場所・費用・メモ付きスケジュールで、当日はチェックで実行済みにできる。概要では予算に対する費用合計と残りを表示。行程の並び替えはハンドルのドラッグ（日をまたいだ移動・端で自動スクロール）と ↑↓ キーの両方に対応。一覧は日程から判定した状態（計画中 / 旅行中 / おもいで）でグループ分けする
- お出かけリストで一覧のカードをタップしたとき、いきなり編集フォームではなく詳細モーダル（`PlaceDetailModal`）を表示するように。住所・タグ・メモ・訪問日・評価・感想・追加した人をまとめて確認でき、そこから「地図で開く」「Web検索」「行った！記録」「編集」へ進める
- 食べたいおかずの一覧で動画をその場で再生できるように（`VideoEmbed`）。YouTube / Shorts / TikTok のカードはサムネイルの再生ボタンから埋め込みプレイヤーが開き、同時再生は 1 本まで。TikTok のサムネイル URL は期限切れで無効になるため、読み込み失敗時はプレースホルダーを出して再生導線を残しつつ `fetch-og-image` で永続 URL に自動で貼り替える
- ホームにアプリ横断検索 `GlobalSearch` を追加。予定・買い物・在庫・おかず・お出かけ・価格をまとめて検索し、結果からそのアプリへ移動できる
- 他アプリから買い物リストへ品物を送る共通モーダル `AddToShoppingListModal` を追加。価格比較の比較シート（最安店と価格をメモに自動セット）と、おかずカードの「買い物へ」から利用できる
- 家族スコープの取得 + Realtime 購読 + 再取得をまとめた `useFamilyData` フックと、取得失敗時の `ErrorNotice`（再読み込み付き）を追加。買い物 / 価格 / 家計 / お出かけ / 在庫 / 旅行の各ページを移行し、これまで無言で失敗していた取得エラーを画面に表示するように
- 共通モーダル `Modal` を追加し、全ページ 20 箇所のモーダルを置き換え。Esc / 背景タップで閉じる・フォーカストラップ・背景スクロールロック・`role="dialog"` を一元的に担保（ページ側のモーダル CSS 約 700 行を削除）
- 描画エラーで画面が白くなるのを防ぐ `ErrorBoundary` を追加（アプリ全体＋ルート単位。再試行 / ホームへ戻る導線付き）
- 買い物リストに未購入・購入済み履歴の「もっと見る」ページネーション、アイテム編集モーダル、重複アイテムの追加防止、購入済み履歴の一括削除を追加。追加操作は楽観的更新に変更し、重要フラグ付きアイテムはリスト先頭に固定表示されるように
- 買い物リストの見た目・使い勝手をさらに磨き込み: 読み込み中のスケルトン表示、重要アイテムの左ボーダー強調、リストタブの未購入件数バッジ、商品名入力時の「よく買う商品」サジェスト、スワイプ削除の追従アニメーションを追加
- 旅行記録に日本地図表示を追加（`JapanMap`）。記録した都道府県を地図上でハイライトし、タップで一覧を絞り込み可能（地図データは静的 SVG として事前生成・バンドルしており、従量課金の地図 API は不使用。出典: 地球地図日本／国土地理院）
- ホームの `FamilyInfo` でメンバーアイコンをタップすると、名前・メールアドレス・参加日を表示する `MemberInfoModal` を開けるように（自分自身には「あなた」バッジを表示）
- お出かけリストで場所名（タイトル）タップ時に、モーダル内へ簡易ブラウザ（Google 検索の iframe 埋め込み）を表示。キーワード編集・外部ブラウザで開くフォールバック付き
- アイコンの正本 `src/lib/icons.js` を追加。Bootstrap Icons を単色・線画で統一し、意味のある別名（`IconHome`, `IconSearch` …）で re-export
- AI 開発ハーネスを導入（`docs/` 配下のドキュメント一式・AI_RULES・CONTRIBUTING・CHANGELOG）
- グローバルナビ（下部タブバー `BottomNav`）を全ページに追加し、アプリ間をホーム経由せず移動可能に
- 共通コンポーネント `ConfirmDialog` / `EmptyState` / `Toast` / `OfflineBanner` を追加
- オフライン検知バナー（リアルタイム同期の状態を明示）
- 家族参加フローに招待リンク貼り付け欄を追加（`GroupSetup`）

### Security
- 招待の仕組みを家族 ID から使い捨ての招待トークン（有効期限 7 日）に変更（マイグレーション 031）。従来は `families` の SELECT が認証済み全員に開放されていたため全家族を列挙でき、`family_members` の INSERT が `user_id` しか検証していなかったため、未所属ユーザーが任意の家族に参加してそのデータを閲覧できる状態だった。家族の作成・参加は `security definer` の RPC 経由のみとし、`families` の SELECT は自分の家族に限定した。招待リンクは `/join/:token` 形式になる
- `push_subscriptions` の WITH CHECK に `family_id = get_my_family_id()` を追加（マイグレーション 030）。従来は他家族の family_id で自分の端末を登録でき、その家族のプッシュ通知（予定タイトル・買い物アイテム名）を受信できる状態だった。既に混入している家族外の購読行も同マイグレーションで削除する
- `get_my_family_id()` に `set search_path` を設定（security definer 関数の基本対策）

### Fixed
- 旅行の行程フォームで「時刻」の入力欄が画面からはみ出していた問題を修正。iOS Safari の日付・時刻入力は内容幅を優先して `width` の指定に従わないため、ネイティブ装飾を外して幅どおりに収まるようにした。あわせて「日」と「時刻」の 2 列を等分から、時刻を必要な幅（8.5rem）に固定して残りを「日」に回す配分に変更（旅行フォームの開始日・終了日も同じ入力を使っているため一緒に直る）
- 入力モーダルが横（や縦）に揺れて操作しにくい問題を修正。オーバーレイが `position: fixed` でレイアウトビューポート基準だったため、iOS でキーボードを出したりピンチしたりして「見えている領域」がずれるとモーダルだけ取り残されていた。`visualViewport` に追従させ、パネルの高さも見えている範囲に収まるようにした（キーボードに隠れず、中でスクロールして保存ボタンまで届く）。あわせてパネルの横スクロール（`overflow-y: auto` の副作用で `overflow-x` も `auto` になっていた）を止め、背面ページへのスクロール伝播も遮断した
- 入力欄をタップするとページが拡大され、画面が横にずれる問題を修正。`index.css` の `input, select, textarea { font-size: 16px }` を各ページの CSS Module が 13〜15px で上書きしていたため、iOS のフォーカス時自動ズームが発動していた（お出かけ・価格・在庫・おかず・買い物・予定のコメント・旅行の計 16 箇所を 16px に）
- 旅行と予定表の連携の取りこぼしを修正。予定表側から旅行の予定を削除した旅行（`schedule_event_id` が外部キーで NULL になる）は、以後どれだけ編集しても予定が作られないままだったため、編集時に予定を作り直して紐付け直すようにした。また旅行の削除時に予定の削除エラーを確認していなかったため、予定の削除だけ失敗すると「✈ …」の予定が予定表に取り残されていた（現在は旅行を削除せず中断して通知する）
- 下部ナビの実際の高さ（65px、safe-area 込みではさらに変動）と、各ページが予約していた固定値（`--bottomnav-h` = 4rem + safe-area）がずれていた問題を修正。ナビを固定配置から通常フローに移し、レイアウトに高さを測らせるようにした（`--bottomnav-h` は不要になったため削除）
- 在庫の「買い物リストに追加」バーが下部ナビに重なっていた問題を修正
- ホームのアプリカードで、右下の矢印が説明文の2行目に重なっていた問題を修正
- ページ見出しが幅の狭い端末で「…」なしに文字の途中で切れていた問題を修正（`text-overflow: ellipsis` は flex コンテナには効かないため、見出しをインラインレイアウトに戻した。予定 / おかず / 価格 / 家計 / 買い物 / お出かけの住所チップが対象）
- 旅行記録と予定表の連携が静かに切れる問題を修正。`travel_trips.schedule_event_id` に外部キー（`ON DELETE SET NULL`）がなく、予定表側から旅行の予定を削除すると存在しない ID を指したまま残り、旅行を編集しても予定へ反映されなくなっていた。旅行の作成・削除も、途中で失敗しても片方だけが残らない順序に変更（マイグレーション 032）
- オフラインバナーの文言を実態に合わせて修正（オフライン中の変更はキューされず失敗するため「オンライン復帰後に同期されます」は誤り）
- ログイン画面などの `100vh` に `100dvh` を併記（iOS Safari でアドレスバー分はみ出していた）
- 繰り返し予定の日付計算がクライアント（ローカル時刻）と Edge Function（UTC）で基準がずれており、JST 9:00 より前に始まる予定では「この回だけ削除」した回にもリマインダーが送られていた問題を修正。Edge Function 側も JST の暦日で計算するようにし、オカレンス計算を `occurrences.ts` に分離。両者の一致は `npm run check:recurrence` で検証できる
- デプロイ直後の古いタブで遅延読み込みのチャンク取得に失敗すると画面が復旧しなかったため、その場合だけ一度自動で再読み込みするように（オフライン時は再読み込みしない）
- 「毎日」の繰り返し予定が、開始から約 800 日を過ぎると予定表に表示されずリマインダーも止まっていた問題を修正。繰り返しの展開を先頭からの総当たりではなく表示範囲からの逆算に変更した（表示範囲に比例した計算量になり、月移動も軽くなる）
- Push 通知の `tag` が `shopping-reminder` 固定だったため、買い物リマインダーと予定リマインダー／予定変更通知が互いを上書きして消えていた問題を修正（種類・イベント単位のタグを Edge Function から送出）

### Changed
- 場所検索（Google Places Autocomplete）の呼び出しを無料枠に十分収まるように制限。1 文字ごとにリクエストが飛ぶ `places.Autocomplete` ウィジェットをやめ、デバウンス（450ms）・最小 2 文字・同一クエリのキャッシュ・セッショントークンをまとめた共通コンポーネント `PlaceSearchInput` に統一（お出かけリストの検索・場所追加・場所編集、旅行の宿泊先の 4 箇所）。加えて端末ごとに 1 日 100 / 1 か月 1,000 リクエストの上限を設け、超えたら候補表示を止めて手入力に切り替える。「9 文字打って 1 件選ぶ」操作で 11 → 2 リクエストになる
- 予定表の月セルで、狭い端末では時刻を出さず予定名を優先するように（時刻が固定幅を取るため、予定名がほとんど表示されず「00:00」だけが並んでいた）
- 在庫カードの品名を 2 行まで折り返すように（右側のボタン列が固定幅のため、長い品名がすぐ切れていた）
- 買い物リストの見出しを 1 行に収め、狭い端末では文字サイズを下げるように（上のタブと同じリスト名が 2 行で重複表示され、縦方向を圧迫していた）
- 狭い端末でヘッダーのタイトルが大きく削られていた問題を改善（320px のホームでは 173px 必要なところに 90px しか与えられていなかった）。400px 以下でヘッダーの余白・間隔・見出しサイズを詰め、ホームのアプリ名は PWA の `short_name` と同じ「家族アプリ」に切り替えるようにした。375px ではすべてのページで見出しが省略なしに収まる（320px では予定表のみ操作ボタンが多く省略される）
- 招待ページの配色をデザイントークンに統一（青紫のグラデーションがハードコードされており、このページだけ「灯 Akari」の配色から外れていた）
- デスクトップ（1024px 以上）の整列を全ページで統一。ヘッダー・検索バー・タブと本文の左端がページ内で最大 4 種類（16 / 150 / 166 / 300px）に割れていたため、全幅バーの `padding-inline` に本文側の内側余白分を足し、すべて同じ縦のラインに揃えた。お出かけでは `max-width` と `padding-inline` が二重に効いて内容が 300px も内側にずれていた。在庫・旅行にはデスクトップの整列指定自体がなかったため追加
- タップ領域が 44px に満たない操作系を全ページで是正（実測で 60 種 → 16 種）。見た目を変えないよう擬似要素で当たり判定だけ広げ、擬似要素を持てない `<input>` は `min-height` で確保した。お出かけの検索欄は高さ 16px しかなかった
- ホームの「今日の予定」を最大 4 件までにし、超えた分は「他 N 件を見る」に畳むように。予定の多い日にアプリ一覧が画面外へ押し出され、ランチャーとして機能していなかった
- `shopping_items(list_id)` など、外部キーと家族スコープの検索に使うカラムへインデックスを追加（マイグレーション 032）。`shopping_items.list_id` は RLS のサブクエリでも毎回評価される
- 配色のコントラストを WCAG AA（4.5:1）基準で見直し。ダークモードではブランド色が明るく反転するため `background: var(--primary)` に `color: #fff` を載せた箇所（削除確認ダイアログのボタン等）が 2.4:1 まで落ちて読めなかった。`--on-primary` などの前景色トークンを追加して 25 箇所を置き換え、あわせて `--primary` / `--danger` / `--ink-3` とブランドグラデーションの値をわずかに濃く調整した
- 2735 行あった `SchedulePage` を分割。定数と日付・繰り返し展開ロジックを `src/lib/schedule.js`（React 非依存の純関数）へ、月 / 週 / アジェンダ表示と各モーダルを `src/components/schedule/` へ切り出し、ページ本体は 817 行に
- 旅行記録の活動記録を旅行ごとの N+1 クエリから 1 クエリのまとめ取得に変更
- 価格比較の取得・Realtime 購読に `family_id` の明示フィルタを追加（従来は RLS 任せ）
- ルート単位でコード分割（`React.lazy` + `Suspense`）。ホーム以外のページを遅延読み込みにし、初回ダウンロードを JS 約 825KB → 約 495KB / CSS 約 212KB → 約 21KB に削減
- お出かけのタグ入力をトグル式マルチ選択に刷新（選択で強調＋チェック表示）。プリセットタグを 12→21 に拡充（居酒屋・絶景・写真映え・海・ビーチ・花見・紅葉・映画・ショッピング・コスパ良し 等）。IME 変換確定の Enter で誤登録しないよう対応。保存失敗時はトーストで通知
- お出かけの場所追加/編集で、場所検索（Places オートコンプリート）を主入力に。場所を選ぶと場所名・住所・地図座標を自動入力し、名前と住所の二度入力を解消（手入力した名前は上書きしない）
- 全画面の絵文字アイコンを Bootstrap Icons（`react-icons/bs`）の単色・線画アイコンに置き換え、派手さを抑えて視認性と統一感を改善（ナビ・ヘッダー・検索・絞り込み・空状態・バッジ・お出かけカテゴリ／目的タグ・地図マーカー・評価の星など）
- 買い物リストの削除を確認ダイアログ経由に変更（従来は確認なしで即削除）
- 削除確認 UI を全ページで `ConfirmDialog` に統一（Places の編集モーダルの即削除も確認付きに）
- 戻るボタンを全ページで 🏠 アイコン・円形 40px・`aria-label` 付きに統一（Inventory / Travel の `←` を置換）
- 主要な操作系のタップ領域を 40〜52px 以上へ拡大（戻る・リスト削除の当たり判定・ダイアログ・タブ）
- お気に入り更新失敗を無言のロールバックからトースト通知に変更
- お出かけリストの操作バーを整理：検索＋「絞り込み」トグル＋ビュー切替の 1 行に集約し、カテゴリ・都道府県・タグ・範囲は折りたたみパネルへ。有効件数バッジと一括解除を追加
- 曜日ヘッダ・「近日公開」バッジの文字サイズを可読性向上のため微増
- スケジュールの「勤務入力モード」をヘッダーの「⋯」メニューに格納し、通常時のヘッダーを簡素化（モード中は明示的な「勤務モード終了」を表示）
- Web フォントの読み込みを CSS `@import`（レンダリングブロッキング）から `index.html` の `preconnect` + `<link>` に移設し初回描画を改善
- 下部ナビ（BottomNav）を `position: fixed` で画面下部に常時固定（従来はレイアウトによりスクロールで動く場合があった）。各ページのスクロール領域はナビ高さ分の余白を確保
- お出かけリストで検索・絞り込み後に一覧の見出しが消え、探索エリアとカードが混在して見えていた問題を修正。常に「検索結果 N件」等の見出しと「条件をクリア」ボタンを表示し、該当なし時も条件クリア導線を出すよう変更
- お出かけリストの「現在地から近い場所を探す」を、独立した横スクロールのプレビュー表示から既存の範囲検索フィルタに統合。検索結果の一覧（見出し・件数）として表示されるよう変更（絞り込みパネル自体は自動で開かず、結果一覧の表示領域を圧迫しないようにした）
- お出かけリストで範囲検索が有効な間、一覧を起点から近い順に並べ替え、各カードに起点からの距離（km）を表示するよう変更

## [2026-07] までの主な機能（初版時点の棚卸し）

### Added
- 家族グループ（Google ログイン・招待リンク参加・1 ユーザー 1 家族）
- 買い物リスト（複数リスト・お気に入り・重要フラグ・購入記録・リアルタイム同期・定時 Push 通知）
- 価格比較（店舗マスタ・小数対応・商品アイコン）
- 家計（カテゴリ別エントリ）
- お出かけリスト（Google Maps 連携・タグ検索・「今日はどこ行く？」導線）
- 食べたいおかず（カテゴリ・OG 画像自動取得と永続化・評価とレビュー）
- 予定表（終日 / 時間指定・勤務シフトモード・変更履歴）
- 在庫管理（3 段階ステータス・賞味期限）
- 旅行記録（旅程・アクティビティ・都道府県）
- デザインシステム「灯 Akari」（ライト / ダークモード・デザイントークン）
- PWA 対応（ホーム画面追加・Service Worker・Web Push）
